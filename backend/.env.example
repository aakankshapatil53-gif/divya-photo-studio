# ── Server ────────────────────────────────────────────────
PORT=4000
FRONTEND_URL=http://localhost:3000

# ── Database ──────────────────────────────────────────────
# PostgreSQL connection string. On Render, create a free "PostgreSQL"
# instance and paste its "Internal Connection String" here (or the
# "External Connection String" if running this locally against it).
# Example:
# DATABASE_URL="postgresql://user:password@host:5432/divya_photo_studio"
DATABASE_URL="postgresql://neondb_owner:npg_5CJuvFASVN1x@ep-fragrant-frost-atfnwepi.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# ── Auth ──────────────────────────────────────────────────
JWT_SECRET="change-this-to-a-long-random-string"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="ChangeMe123!"

# ── Email notifications (optional) ───────────────────────
# Uses Gmail SMTP with an App Password. Leave blank to skip emails
# (bookings still save to the database and show in the admin panel).
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=465
SMTP_USER="divyahdphotostudio@gmail.com"
SMTP_PASS=""
NOTIFY_EMAIL="divyahdphotostudio@gmail.com"

# ── Cloudinary (optional, for gallery uploads via admin panel) ──
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
